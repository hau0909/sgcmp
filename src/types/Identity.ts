export interface Identity {
  identity_id: string;
  user_id: string;
  issue_date: string;
  issue_place: string;
  front_url: string;
  back_url: string;
  created_at: string;
  updated_at: string;
}
